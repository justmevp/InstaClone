// material-ui
import { Typography } from '@mui/material';

// project import
import MainCard from 'components/MainCard';

const SamplePage = () => {
  return (
    <MainCard title="About">
      <Typography variant="body2">
        Responsible by justmevp
      </Typography>
    </MainCard>
  );
};

export default SamplePage;
